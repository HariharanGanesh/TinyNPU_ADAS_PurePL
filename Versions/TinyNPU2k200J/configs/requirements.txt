# requirements.txt — TinyNPU Python Dependencies

# Core numerics (golden model + test infrastructure)
numpy>=1.24.0

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0          # Coverage reporting

# RTL simulation (cocotb)
cocotb>=1.8.0
cocotbext-axi>=0.1.20      # AXI BFMs for cocotb (Master/Slave/Monitor)

# Optional: visualization and analysis
matplotlib>=3.7.0           # Plotting throughput/latency results
pandas>=2.0.0               # Results tabulation

# Optional: NN framework integration (for loading quantized models)
# Uncomment when needed:
# tensorflow>=2.13.0        # For loading TFLite INT8 models
# onnx>=1.14.0              # For loading ONNX models
# onnxruntime>=1.16.0       # For ONNX runtime golden model comparison
